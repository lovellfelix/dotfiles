aliases:
  dp: deployments
  sec: v1/secrets
  jo: jobs
  cr: clusterroles
  crb: clusterrolebindings
  ro: roles
  rb: rolebindings
  np: networkpolicies
  hass: pod app.kubernetes.io/name=home-assistant -n default
  metallb: pod app.kubernetes.io/name=metallb -n kube-system
  nginx: pod app.kubernetes.io/instance=nginx-ingress -n kube-system
  vpn: pod app.kubernetes.io/name=pod-gateway -n vpn
